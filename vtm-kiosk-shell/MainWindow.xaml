<Window x:Class="VtmKioskShell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="VTM Kiosk"
        WindowState="Maximized"
        WindowStyle="None"
        Background="#0b0d10">

    <Grid>
        <!-- Shown until the page has loaded, so a kiosk never starts on a white rectangle. -->
        <TextBlock x:Name="Splash"
                   Text="Starting…"
                   Foreground="#9aa3ae"
                   FontSize="20"
                   FontFamily="Segoe UI"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />

        <wv2:WebView2 x:Name="Web" Visibility="Hidden" />

        <!-- A kiosk that cannot reach its page must say so rather than sit blank. -->
        <TextBlock x:Name="Error"
                   Foreground="#e5484d"
                   FontSize="18"
                   FontFamily="Segoe UI"
                   TextWrapping="Wrap"
                   MaxWidth="640"
                   TextAlignment="Center"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed" />
    </Grid>
</Window>
